

<% for(let i = 0; i < kontakt.length; i++) { %>
    <div>
        <p id="footer_p"> 
            <%= kontakt[i].firmanavn%>, 
            <%= kontakt[i].adresse %>, 
            <%= kontakt[i].postnr %>, 
            <%= kontakt[i].telefon %>,
            <%= kontakt[i].email %>
        </p>
    </div>
    <%}%>